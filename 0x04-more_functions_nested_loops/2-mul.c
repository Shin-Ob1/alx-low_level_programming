#include "main.h"

/**
 *mul-prints the absolute value of an integer.
 *@a:the integer to compute its absolute value.
 * @b: the other integer
 * Return: the multiplication of a and b
 */
int mul(int a, int b)
{
	int x = a * b;

	return (x);
}
